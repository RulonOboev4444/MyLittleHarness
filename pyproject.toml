[build-system]
requires = []
build-backend = "mylittleharness_build"
backend-path = ["build_backend"]

[project]
name = "mylittleharness"
version = "1.0.0"
description = "MyLittleHarness workflow analyzer packaged with primary init/check/repair/detach CLI posture, explicit bootstrap package-smoke verification, unified source-verified intelligence query search with cold archived-plan/reference retrieval and disposable navigation-cache refresh, marker-only detach apply, read-only rule/context, decision, ADR, and roadmap routes, hidden deterministic command intent suggestions with JSON advisory output, hidden intake route advisor for incoming text, optional proof/evidence and agent run evidence route records, route metadata, relationship hygiene scan diagnostics and incubation cleanup advisor classifications with generated relationship graph projection and product-target artifact status, text audit and entry coverage suggestions, remainder drift diagnostics, report-only grain diagnostics and archived-plan calibration samples, hidden-help advanced tasks/bootstrap diagnostics, semantic readiness and no-runtime evaluation reports, optional preflight warnings and hook templates, MCP read-projection adapter inspection and stdio serving, report-only evidence hardening, closeout quality cues and Git trailer suggestions, explicit closeout/state/product_source_root writeback sync with project-state closeout authority fallback and plan-identity carry/replace guardrail plus phase-only uncertain-docs replacement, explicit ready-for-closeout boundary, and completed-plan archive close, source-incubation relationship writeback, coverage-aware incubation auto-archive, plus post-writeback and compact-only whole-state compaction, hidden incubate same-topic note rail with fix-candidate tagging, hidden deterministic plan synthesis rail with roadmap-derived title/objective/task input, roadmap slice frontmatter plus current-phase-only execution metadata, explicit auto_continue stop-condition diagnostics, bounded plan-synthesis rationale, target-artifact pressure reporting, and docs write-scope impact reporting, hidden memory lifecycle hygiene rail with archive-covered Entry Coverage transactions, hidden bounded roadmap item mutation rail with reciprocal source-incubation metadata, non-owning source-incubation reuse provenance, relationship frontmatter block recovery, and advisory execution-slice fields, hidden plan/writeback roadmap relationship sync with covered-item writeback batching and source-incubation active-plan refresh, snapshot inspection, create-only AGENTS/docmap/stable-spec repair, and snapshot-protected state frontmatter repair"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
mylittleharness = "mylittleharness.cli:main"
