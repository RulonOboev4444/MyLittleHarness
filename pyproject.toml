[build-system]
requires = []
build-backend = "mylittleharness_build"
backend-path = ["build_backend"]

[project]
name = "mylittleharness"
version = "1.0.0"
description = "MyLittleHarness workflow analyzer packaged with primary init/check/repair/detach CLI posture, explicit bootstrap package-smoke verification, unified source-verified intelligence query search with disposable navigation-cache refresh, marker-only detach apply, read-only rule/context, decision, ADR, and roadmap routes, optional proof/evidence route records, route metadata, relationship hygiene scan diagnostics with generated relationship graph projection, text audit and entry coverage suggestions, remainder drift diagnostics, report-only grain diagnostics and archived-plan calibration samples, hidden-help advanced tasks/bootstrap diagnostics, semantic readiness and no-runtime evaluation reports, optional preflight warnings and hook templates, MCP read-projection adapter inspection and stdio serving, report-only evidence hardening, closeout quality cues and Git trailer suggestions, explicit closeout/state writeback sync with plan-identity carry/replace guardrail, explicit ready-for-closeout boundary, and completed-plan archive close, source-incubation relationship writeback, coverage-aware incubation auto-archive, plus post-writeback and compact-only state compaction, hidden incubate same-topic note rail, hidden deterministic plan synthesis rail with roadmap slice frontmatter plus current-phase-only execution metadata, explicit auto_continue stop-condition diagnostics, and bounded plan-synthesis rationale and target-artifact pressure reporting, hidden memory lifecycle hygiene rail, hidden bounded roadmap item mutation rail with reciprocal source-incubation metadata and advisory execution-slice fields, hidden plan/writeback roadmap relationship sync with covered-item writeback batching, snapshot inspection, create-only AGENTS/docmap/stable-spec repair, and snapshot-protected state frontmatter repair"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
mylittleharness = "mylittleharness.cli:main"
